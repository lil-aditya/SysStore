apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-service
  namespace: cloudnative
  labels:
    app: file-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: file-service
  template:
    metadata:
      labels:
        app: file-service
    spec:
      containers:
        - name: file-service
          image: cloudnative-file-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
          envFrom:
            - configMapRef:
                name: cloudnative-config
            - secretRef:
                name: cloudnative-secrets
          volumeMounts:
            - name: file-storage
              mountPath: /root/uploads
          livenessProbe:
            httpGet:
              path: /api/v1/file-service/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /api/v1/file-service/health
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "250m"
      volumes:
        - name: file-storage
          persistentVolumeClaim:
            claimName: file-uploads-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: file-service
  namespace: cloudnative
  labels:
    app: file-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
  selector:
    app: file-service
---

